using System;
using Terminal.Gui;
using BulkRename.Class;

// use editor to display all files in the given directory
// and then for each line of the "text file" run a foreach loop to compare the original against the current value of the text file and rename it if it's different.
// or track which lines get modified and run rename on the changes

// check if Directory class has the ability to sort when getting all files of a directory
// also look into how to do multiline edits with terminal gui's editor
namespace BulkRename.Tui
{
    public class App
    {
    }
}
